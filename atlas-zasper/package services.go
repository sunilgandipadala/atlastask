package main

/*
type Connector Interface {
    ExecuteReq()
}

===================================
type ApiConnector struct{
	IApiConnector //interfaces

	here if it is not possible, we can pass the structs defined separately for implementation of these interface may use i Guess.
}
func (connect *ApiConnector) ExecuteReq(){
	//ExectuteAPI Connector Body

	//In this, instead of directly calling the interface methods just replace them with,
	connect.IApiConnector.MethodName()
}
===================================

type DbConnector struct{
	IDbConnector //interfaces

	here if it is not possible, we can pass the structs defined separately for implementation of these interface may use i Guess.
}

func (connect *DbConnector) ExecuteReq(){
	//ExectreDB Connector Body
	//In this, instead of directly calling the interface methods just replace them with,
	connect.IDbConnector.MethodName()
}

===================================
func NewConnector(connectionType) Connector {
	//Based on the Connection Type .. pass the struct object
	if typecheck {
		return &ApiConnector(dataConnection.--)
	}else {
		return &DbConnector(dataConnection.--)
	}
}

===================================

connector := NewConnector(connectionType)
connector.ExecuteReq()
*/
